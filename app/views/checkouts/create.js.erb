var stripe = Stripe("<%= Stripe.api_key = Rails.application.credentials.stripe[:public_key] %>");

stripe.redirectToCheckout({
    sessionId: '<%= @session.id %>'
}).then(function (result) {
    console.log(result.error.message);
});